"use client";

/**
 * Backward-compatible chat bootstrapper.
 * The site now lazy-loads Brevo on explicit user action. This component stays
 * in place only so older imports remain harmless.
 */
export function ZohoChat() {
  return null;
}
